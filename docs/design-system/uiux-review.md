# 뭉실 전체 UI/UX 점검 기록

점검일: 2026-08-30  
기준 문서: `docs/design-system/cloud-comfort-product.md`  
검증 화면: 360×800, 390×844, 430×900 모바일 및 데스크톱 중앙 프레임

## 1. 이번 점검 범위

공개 랜딩과 인증 화면, 5개 핵심 탭, TODO 가져오기, 챌린지·대화방·1:1 대화·게시물·사용자 상세, 알림·설정·약관, 운영자 화면까지 현재 존재하는 24개 라우트를 검토했다.

- `/`, `/start`, `/forgot-password`, `/reset-password`, `/verify-email`
- `/onboarding`, `/today`, `/explore`, `/todos`, `/todos/import`
- `/challenges`, `/challenges/[id]`, `/challenges/[id]/chat`, `/me`, `/notifications`, `/settings`
- `/posts/[id]`, `/people/[handle]`, `/people/[handle]/connections`
- `/messages`, `/messages/[id]`
- `/privacy`, `/terms`, `/admin/reports`

브라우저 점검에서는 세 모바일 폭 모두에서 문서 전체의 가로 넘침이 없었고, 제품 화면의 세로 스크롤은 `.app-scroll` 하나로 유지됐다. 시트는 별도 포털 레이어와 `.sheet-body` 내부 스크롤을 사용한다.

## 2. 이번에 확인하고 수정한 공통 결함

### TODO 가져오기 스크롤

- 원인: 하단 저장 CTA가 `position:absolute`여서 폼 높이에 포함되지 않았고 화면 높이에 따라 마지막 입력을 덮거나 스크롤 범위가 부족해졌다.
- 수정: 폼을 세로 flex 흐름으로 바꾸고 CTA를 문서 흐름에 포함된 `position:sticky`로 전환했다.
- 기준: 짧은 루틴 가져오기는 CTA가 화면 하단에 머물고, 긴 단일 TODO 가져오기는 모든 카테고리·반복 항목 뒤까지 자연스럽게 스크롤돼야 한다.

### 루틴 가져오기 반복

- 단일 TODO뿐 아니라 루틴에도 반복 정책을 노출했다.
- 루틴은 `원본 유지 · 반복 없이 · 항목별 설정`을 제공하고, 항목별 설정에서는 각 TODO의 이름·날짜·시간·반복을 조정한다.
- 서버는 원본 항목이 아닌 ID를 거부하고, 선택한 정책대로 반복 시리즈 또는 단일 TODO를 생성한다.

### 스레드형 TODO 그룹

- 오늘과 TODO 화면에 기존 루틴 묶음을 접을 수 있는 그룹으로 표시한다.
- 접힌 그룹에서도 완료율과 다음 TODO를 보여주며, 펼치면 실행 순서를 세로 연결선으로 표현한다.
- 반복 일정은 `seriesId`로 같은 그룹에 이어지고 한 TODO 또는 반복 시리즈가 여러 그룹에 중복되지 않도록 생성·편집 단계에서 막는다.

### Bottom Sheet

- 앱 셸 내부 transform과 overflow로 시트가 잘리던 문제를 `document.body` 포털로 해결했다.
- 시트 헤더와 닫기는 고정하고 본문만 스크롤한다.
- 앱 배경은 `inert`, 배경과 앱 스크롤은 잠그며 닫은 뒤 원래 포커스를 복구한다.
- 스크롤바와 입력 사이에 여백을 두고 뭉실 색상의 얇은 스크롤바를 사용한다.

### 브라우저 기본 UI

- TODO 작성·편집·가져오기의 날짜, 시간, 카테고리, 반복을 공통 전용 UI로 교체했다.
- 게시 공개 범위는 `전체 공개 · 팔로워 · 나만 보기` 전용 선택 카드로 교체했다.
- `게시`와 외부 `공유` 용어를 분리했다.

### 터치 영역

- 브랜드 링크, 인증 전환 버튼, 약관 돌아가기 링크를 최소 44px 높이로 보강했다.
- 360px TODO 주간 달력은 카드 폭을 안전하게 확장해 날짜 버튼의 터치 폭을 확보했다.

## 3. 라우트별 검토 결과

| 화면 | 현재 상태 | 확인 내용 | 다음 개선 |
| --- | --- | --- | --- |
| 랜딩 `/` | 양호 | 핵심 문장, 공개 탐색, 모바일 재배치 | 실제 베타 전환 문구 최종 검수 |
| 가입·로그인 `/start` | 양호 | 오류·메일 발송 상태 분리, 터치 영역, 전용 생년월일 선택기 | 실제 가입·인증 E2E |
| 비밀번호·메일 인증 | 양호 | 단일 과업, 오류·재시도 상태 | 실제 만료 토큰 문구 E2E |
| 온보딩 | 양호 | 관심사 선택, 단일 CTA, 단일 스크롤 | 200% 확대 수동 확인 |
| 오늘 | 양호 | 현재 TODO 우선, 상세 편집, 스레드형 그룹, 단일 추가 진입점, 게시 시트 | 실제 계정 완료→게시 E2E |
| 탐색 | 고도화 완료 | 미니멀 기본 피드, 전용 필터 시트, `/explore/search` 통합 검색, 최근 검색, 인기 태그, 가져오기 위계 | 실제 데이터 검색 정확도와 느린 이미지 수동 확인 |
| TODO | 양호 | 주/월 전환, URL 날짜, 편집·복구, 접이식 루틴 그룹과 관리 | 360px 월간 달력 긴 월 검수 |
| TODO 가져오기 | 수정 완료 | CTA 문서 흐름, 루틴 반복 정책, 항목별 일정·반복, 공통 선택기 | 키보드가 열린 360px 실기기 확인 |
| 챌린지 목록 | 고도화 완료 | 참여/공식/커뮤니티 구조, 헤더 생성 진입점, 인증 방식과 오늘 상태 | 실제 다계정 참여 수동 검증 |
| 챌린지 작성 | 고도화 완료 | 간편 체크·참여자 사진 인증 2가지, 기준 1~5개, 전용 날짜·완주율, 순위 칭호와 공식 보상 입력 | 360px 기준 5개 긴 문구 실기기 확인 |
| 챌린지 상세 | 고도화 완료 | 사진 원본 비율 중심의 익명 참여자 투표, 기준별 단계 진행과 64px 선택 카드, 진행 수, 24시간 재제출, 강화 재검증, 순위·칭호·보상 | 실제 8계정 투표 임계값 E2E |
| 챌린지 대화방 | 베타 구현 완료 | 참여자 전용 과거 기록, 사진·답장·수정·삭제·반응, 실시간 갱신, 방별 알림, 차단 접기, 종료 읽기 전용, 방장 관리 | 실제 3계정 소켓 재접속·차단·90일 삭제 E2E |
| 통합 대화함·1:1 대화 | 베타 구현 완료 | 요청 48px lavender 단일 행과 처리 시트, 1:1·참여 챌린지 최신순 통합 목록, 종류·종료 라벨, 프로필 메시지 요청, 텍스트·사진·답장·반응·삭제, 안 읽은 수, 차단·신고, 집중형 채팅 화면 | 실제 2계정 요청 수락·소켓 재접속·사진 실패·차단 및 챌린지 목록 정렬 E2E |
| 마이 | 양호 | 프로필·등급·통계·게시 기록 | 긴 소개와 큰 숫자 검수 |
| 알림 | 양호 | 빈 상태·대상 링크·페이지네이션, 방별 단일 채팅 알림과 안 읽은 수 | 개별 읽음 상태 추가 시 재검수 |
| 게시물 상세 | 양호 | 피드·댓글·가져오기, 게시물·댓글 삭제 확인 시트 | 실제 두 계정 댓글·삭제 E2E |
| 공개 프로필·연결 | 양호 | 팔로우·신고·차단·게시 기록 | 차단/비공개 실제 권한 E2E |
| 설정 | 양호 | 운영자·약관·로그아웃·탈퇴 | 탈퇴 진행 상태 실제 API 검수 |
| 약관·개인정보 | UI 양호 | 읽기 폭과 돌아가기 터치 영역 | 베타용 실제 법률 문안 필요 |
| 운영 센터 | 베타 운영 기능 완료 | 사용자·활성·핵심 순환 현황, 초대 코드, 사용자 제재, 신고 사진·신고 메시지 앞뒤 3개 확인과 숨김, 사진 인증 지연·풀 부족 모니터링(개별 판정 금지), 공식 보상, 감사 기록 | 실제 DB에서 역할·제재·복구와 인증 현황 E2E |

## 4. 남은 UI/UX 우선순위

### P1 — 베타 전에 수정

1. TODO 가져오기를 모바일 키보드가 열린 상태에서 실기기 또는 동일 높이 환경으로 재검증한다.

### P2 — 베타 운영 준비

1. 200% 확대, 스크린리더, 느린 네트워크, 오프라인 상태를 페이지별로 수동 검증한다.
2. 개인정보 처리방침과 이용약관을 실제 베타 문안으로 교체한다.
3. 이미지 업로드 실패 후 재시도와 큰 이미지 진행률을 실제 스토리지로 검증한다.
4. 실제 관리자 계정으로 초대 코드 발급, 사용자 정지, 게시물 숨김, 감사 기록을 한 번에 검증한다.
5. 실제 3계정으로 대화방 과거 기록, 소켓 재접속, 사진 일부 실패, 방장 채팅 제한과 종료 읽기 전용을 검증한다.
6. 실제 2계정으로 메시지 요청·수락, 상호 팔로우 즉시 대화, 읽음 위치, 차단 뒤 쓰기 거부를 검증한다.

## 5. 회귀 방지 검사

다음 패턴은 코드 검사와 리뷰에서 실패로 처리한다.

- 사용자 화면의 `window.confirm`, `window.alert`, `window.prompt`
- 사용자 화면의 새 native `select`, `input[type=date]`, `input[type=time]`
- 긴 폼 CTA의 `position:absolute`
- 44px 미만의 독립 버튼·링크·입력
- 앱 셸과 페이지가 동시에 세로 스크롤되는 구조
- Bottom Sheet를 공통 `Sheet` 밖에서 직접 구현하는 코드
- 내부 발행 동작을 `공유`라고 표시하는 문구

새 UI 작업을 완료할 때 이 문서의 라우트 표와 `AGENTS.md`의 완료 절차를 함께 확인한다.
