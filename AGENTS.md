# 뭉실 개발 에이전트 규칙

이 파일은 저장소 전체에 적용된다. 프론트 UI/UX를 변경하기 전에는 반드시 다음 문서를 먼저 읽는다.

1. `docs/design-system/cloud-comfort-product.md` — 제품 방향과 디자인 시스템의 단일 기준
2. `docs/design-system/uiux-review.md` — 현재 라우트별 점검 결과와 남은 개선 항목

## 제품 기준

- 핵심 순환은 `TODO 생성 → 완료 → 게시 → 응원·댓글 → 가져오기 → 다시 완료`다.
- 제품 탭은 `오늘 · 탐색 · TODO · 챌린지 · 마이`를 유지한다.
- 내부 피드 발행은 `게시`, 외부 링크 전송만 `공유`라고 부른다.
- 한 화면에서 가장 강한 주 행동은 하나만 둔다.
- 20~40대용 차분한 감성 라이프스타일 제품을 지향하며 귀여움은 완료·빈 상태·등급 상승에만 절제해서 사용한다.

## 스크롤과 레이어 계약

- 일반 제품 화면의 세로 스크롤 소유자는 `.app-scroll` 하나다. 페이지 안에 불필요한 이중 세로 스크롤을 만들지 않는다.
- 긴 폼의 CTA를 `position:absolute`로 배치하지 않는다. CTA는 문서 흐름에 포함하고 필요하면 `position:sticky`와 Safe Area를 사용한다.
- 하단 고정 CTA를 사용할 때는 마지막 입력과 CTA가 겹치지 않는지 360px 높이와 모바일 키보드 상태에서 확인한다.
- Bottom Sheet는 반드시 공통 `Sheet`를 사용한다. `document.body` 포털, 배경 `inert`, 스크롤 잠금, 포커스 트랩·복귀, Esc·배경 클릭, Safe Area, 내부 본문 스크롤 계약을 우회하지 않는다.
- 시트 헤더는 고정하고 `.sheet-body`만 스크롤한다. 스크롤바와 입력 사이에는 최소 8px 여백을 둔다.
- 앱 셸의 transform/overflow 안에 `position:fixed` 모달을 직접 렌더링하지 않는다.
- 챌린지 대화방도 `.app-scroll`을 유일한 세로 스크롤로 사용한다. 새 메시지 수신으로 사용자가 보고 있던 과거 위치를 강제로 바꾸지 않고, sticky 작성창은 Safe Area와 모바일 키보드 상태에서 마지막 메시지를 가리지 않아야 한다.
- 1:1 대화 상세도 같은 스크롤·작성창 계약을 지키고 하단 5탭을 숨긴다. 대화 목록은 일반 제품 셸 안에 두며 5개 핵심 탭 자체를 메시지 탭으로 교체하지 않는다.
- 대화함에서는 메시지 요청을 한 줄 요약과 공통 `Sheet`로 처리한다. 1:1과 참여한 챌린지 방은 최신 활동순 한 목록으로 합치고 `1:1·챌린지·종료됨`을 텍스트로도 구분한다.

## 입력과 확인 UI 계약

- 사용자 화면에서 브라우저 기본 `select`, 날짜/시간 피커, `window.alert`, `window.confirm`, `window.prompt`를 새로 사용하지 않는다.
- 날짜·시간·카테고리·반복은 `todo-form-controls.tsx`의 공통 패턴을 재사용하거나 같은 접근성 계약의 공통 컴포넌트로 확장한다.
- 선택 UI는 버튼 또는 listbox로 구현하고 텍스트·아이콘·선택 표시를 함께 제공한다. 선택 상태는 `aria-pressed` 또는 올바른 ARIA 역할로 노출한다.
- 삭제·참여 취소처럼 되돌리기 어려운 행동은 뭉실 전용 확인 시트를 사용한다.
- 텍스트 입력은 항상 보이는 레이블을 가지며 도움말과 오류는 해당 입력 바로 아래에 둔다.
- 참여자 사진 인증 검수는 사진을 불필요한 정사각형 여백에 가두지 않고 한 기준씩 진행한다. 판정 선택지는 아이콘·제목·설명을 포함한 최소 60px 카드로 제공하며 현재 단계와 이전 이동을 표시한다.

## 모바일·접근성 계약

- 구현과 검증 기준은 360px, 390px, 430px 모바일과 PC 중앙 430px 프레임이다.
- 모든 버튼·링크·입력의 터치 영역은 최소 44×44px이다. 보이는 아이콘 버튼에는 한국어 접근성 이름을 제공한다.
- 가로 스크롤, 본문 잘림, 하단 탭·CTA 겹침, 키보드 가림, 200% 확대 잘림을 허용하지 않는다.
- 색만으로 선택·완료·오류를 전달하지 않는다. 텍스트, 아이콘 또는 형태를 함께 사용한다.
- 키보드 Tab, 방향키, Enter, Esc 동작과 `focus-visible`을 확인한다.
- 모션은 120/220/360ms 토큰과 `prefers-reduced-motion`을 지킨다.

## 상태와 데이터 계약

- 정상·로딩·빈 상태·오류·오프라인·권한 없음 중 해당 상태를 모든 화면에 제공한다.
- 낙관적 업데이트 실패 시 데이터를 원복하고 사용자가 다시 시도할 방법을 보여준다.
- 채팅 변경은 REST 저장 성공 뒤 소켓 이벤트로 갱신하고, 연결이 끊기면 주기적 조회로 복구한다. 종료·탈퇴·채팅 제한 상태는 화면 표시만 믿지 않고 서버의 모든 변경 요청에서 다시 검사한다.
- 1:1 대화 요청·전송·답장·반응에서도 양방향 차단 관계와 두 사용자 모두의 대화 멤버십을 서버에서 다시 검사한다. 차단 뒤에는 기록을 지우지 않고 변경 요청만 거부한다.
- 화면 예시 데이터와 실제 API 응답을 한 화면에서 섞지 않는다.
- 사용자 선택 날짜와 필터는 가능하면 URL에 보존한다.
- TODO 작성·편집·가져오기는 동일한 필드 순서와 공통 선택 컴포넌트를 사용한다.
- 루틴 가져오기는 단일 TODO처럼 반복 정책을 확인할 수 있어야 한다. 여러 항목의 반복이 다르면 `원본 유지·반복 없음·항목별 설정`을 구분하고 하나의 값으로 강제 덮어쓰지 않는다.
- TODO 그룹은 기존 `TodoList`를 기준으로 표시하고, 반복 발생 TODO는 `seriesId`로 같은 그룹에 이어 붙인다. 한 TODO 또는 반복 시리즈는 기본 그룹 하나에만 속한다.
- 오늘과 TODO 화면의 그룹은 접힌 상태에서도 이름, 완료율, 다음 TODO를 보여줘 숨겨진 할 일을 예측할 수 있게 한다.
- TODO 일정 화면은 선택 날짜의 할 일을 모두 펼쳐 보여주고, 같은 그룹은 얇은 경계와 작은 라벨로만 묶어 목록보다 강하게 보이지 않게 한다. 그룹의 생성·구성 변경은 `그룹` 관리 화면에서 처리한다.
- 카테고리 기본 화면은 갤러리로 보여주고 순서 이동 조작을 섞지 않는다. 정렬은 공통 시트의 전용 모드에서 드래그와 키보드 방향키·Home·End, 맨 위·맨 아래 이동을 함께 제공한다.
- 카테고리 갤러리의 넓은 카드 영역은 소속 TODO 목록을 여는 진입점이다. 편집·숨김은 별도 보조 행동으로 두고, 목록 시트에서는 전체·예정·완료 상태와 일정 화면으로 이동할 날짜를 제공한다.

## UI 변경 완료 절차

1. 영향받는 화면과 공통 컴포넌트를 먼저 확인한다.
2. 정상 상태뿐 아니라 가장 긴 콘텐츠, 빈 상태, 오류 상태를 확인한다.
3. 360/390/430px에서 가로 넘침과 세로 스크롤 소유자를 검사한다.
4. 터치 영역, 포커스, 레이블, 모달 종료와 포커스 복귀를 검사한다.
5. `npm run lint`, `npm test`, `npm run build`를 통과시킨다.
6. 새로운 재발 가능성이 발견되면 이 파일과 `docs/design-system/uiux-review.md`를 함께 갱신한다.

## 현재 금지된 임시 해결

- 긴 콘텐츠의 높이를 고정값으로 잘라 맞추기
- 모달의 z-index만 올리고 포털·스크롤 소유권 문제를 남기기
- 동일 기능을 화면마다 별도 native picker로 구현하기
- 서로 다른 위치에 같은 `추가` 주 행동을 중복 배치하기
- 오류를 콘솔에만 남기거나 사용자에게 `Internal server error` 그대로 노출하기
